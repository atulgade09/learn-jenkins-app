pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "this is sh cmd"'
                sh 'whoami'
            }
        }
    }
}
